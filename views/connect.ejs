<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Published Websites</title>
    <!-- AOS Animation Library -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css" rel="stylesheet">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
         /* Global Styles */
         body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f7f6;
            color: #333;
        }
        .navbar {
            background-color: #343a40;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            z-index: 10;
        }

        .navbar-brand {
            color: #fff !important;
            font-size: 1.8rem;
            font-weight: 700;
        }

        .navbar-nav .nav-item .nav-link {
            color: #fff !important;
            font-size: 1.1rem;
            padding: 0.7rem 1rem;
        }

        .navbar-nav .nav-item .nav-link:hover {
            color: #f1c40f !important;
        }

        .navbar-toggler {
            border-color: #fff;
        }

        .navbar-toggler-icon {
            background-color: #fff;
        }

        h1 {
            text-align: center;
            margin-top: 30px;
            font-size: 2.5rem;
            color: #333;
            text-transform: uppercase;
            font-weight: 700;
            animation: fadeInUp 1s ease-out;
        }
        .video-container {
            position: fixed; /* Fixes the video to the screen */
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh; /* Full viewport height */
            z-index: -1; /* Ensure it's behind the content */
        }

        #bg-video {
            width: 100%;
            height: 100%;
            object-fit: cover; /* Ensures the video fills the screen */
        }

        .card-container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            justify-content: center;
            padding: 20px;
            animation: fadeIn 1s ease-out;
        }

        .card {
            width: 300px;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            background-color: white;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            margin-bottom: 20px;
            background-color: #fff;
            text-align: center;
            padding: 20px;
            box-sizing: border-box;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
        }

        .card img {
            max-width: 100%;
            height: auto;
            border-radius: 8px;
            margin-bottom: 15px;
        }

        .card h3 {
            margin: 0;
            font-size: 1.6rem;
            color: #333;
            margin-bottom: 10px;
            font-weight: 600;
        }

        .card p {
            font-size: 14px;
            color: #666;
            margin-bottom: 20px;
        }

        .card a {
            text-decoration: none;
        }

        .card button {
            background-color: #007bff;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            width: 100%;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .card button:hover {
            background-color: #0056b3;
        }

        .earnings {
            font-weight: bold;
            color: #28a745;
            margin-top: 10px;
        }

        .order-now {
            background-color: #28a745;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            width: 100%;
            margin-top: 10px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .order-now:hover {
            background-color: #218838;
        }

        /* Animation */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Loading State */
        .loading {
            text-align: center;
            font-size: 1.5rem;
            margin-top: 50px;
        }
    </style>
</head>
<body>
    <div class="video-container">
        <video autoplay muted loop id="bg-video">
            <source src="/33628-397860881_small.mp4" type="video/mp4">
            Your browser does not support the video tag.
        </video>
    </div>

    <audio autoplay loop>
        <source src="/The Weeknd - Blinding Lights (Official Audio).mp3" type="audio/mp3">
        Your browser does not support the audio element.
    </audio>

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <a class="navbar-brand" href="#">Website World</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
                <li class="nav-item"><a class="nav-link" href="/shop">Shop</a></li>
                <li class="nav-item"><a class="nav-link" href="/earn">Earn</a></li>
                <li class="nav-item"><a class="nav-link" href="/all-websites">All Websites</a></li>
                <li class="nav-item"><a class="nav-link" href="/contract">Contract</a></li>
                
                <% if (user) { %>
                    <li class="nav-item"><a class="nav-link" href="/logout">Logout</a></li>
                <% } else { %>
                    <li class="nav-item"><a class="nav-link" href="/login">Log In</a></li>
                <% } %>
            </ul>
        </div>
    </nav>

    <h1 data-aos="fade-up">Profiles</h1>

    <div class="profile-container">
        <h1>Welcome, <%= connect.name %>!</h1>
        <div class="profile-details">
            <img src="<%= connect.image %>" alt="Profile Picture" class="profile-image">
            <p><strong>LinkedIn:</strong> <a href="<%= connect.linkedin %>" target="_blank"><%= connect.linkedin %></a></p>
            <p><strong>GitHub:</strong> <a href="<%= connect.github %>" target="_blank"><%= connect.github %></a></p>
            <p><strong>MetaMask:</strong> <%= connect.metamask %></p>
        </div>
        <a href="/connect">Go Back to Connect</a>
    </div>

    <!-- AOS and JS scripts -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.js"></script>
    <script>
        AOS.init({
            duration: 1000,
            once: true
        });
    </script>
</body>
</html>
